1. **P2** — [src/store/selectors.ts](/Users/kacpercichyn2/Documents/N2click/src/store/selectors.ts:312), [src/pages/TimelinePage.tsx](/Users/kacpercichyn2/Documents/N2click/src/pages/TimelinePage.tsx:273)  
   **Issue:** `conflictDatesForTask` treats bin entries (`date === ''`) as real dates. If a person has more bin hours than capacity, it adds `''` as a conflict date; Timeline then runs `diffDays(task.startDate, '')`, producing `NaN` conflict marker positions and false overload warnings.  
   **Suggested fix:** skip `isBinEntry(w)` in `conflictDatesForTask`, and preferably make per-date selectors return `0`/empty for `BIN_DATE`. Add a selector test for bin hours not creating date conflicts.

2. **P2** — [src/components/TaskModal.tsx](/Users/kacpercichyn2/Documents/N2click/src/components/TaskModal.tsx:416), [src/store/AppStore.tsx](/Users/kacpercichyn2/Documents/N2click/src/store/AppStore.tsx:284)  
   **Issue:** Unassigning a person only prompts about dated allocation hours. Existing bin hours for that person disappear from the editor and are dropped on save by `saveTask`, with no warning.  
   **Suggested fix:** include existing and pending bin hours in the unassign confirmation, and clear `pendingUnassigned`/`binPersonId` for that person when unassignment is confirmed.

3. **P2** — [src/components/WeekView.tsx](/Users/kacpercichyn2/Documents/N2click/src/components/WeekView.tsx:365)  
   **Issue:** Bin-card drag validation only checks the horizontal day column. Drops on the sticky header, below the day body, or on the sticky hour axis while horizontally scrolled can still schedule the block because `relY` is clamped into a valid time.  
   **Suggested fix:** require the pointer to be over the visible day body, not just column `0..6`. Use the scroll container/header/body bounds or hit-test the day body before dispatching `SET_BLOCK_TIME`.

4. **P2** — [src/App.tsx](/Users/kacpercichyn2/Documents/N2click/src/App.tsx:44), [src/utils/uiPrefs.ts](/Users/kacpercichyn2/Documents/N2click/src/utils/uiPrefs.ts:1)  
   **Issue:** `App.tsx` imports `./utils/uiPrefs`, but `src/utils/uiPrefs.ts` is currently untracked and absent from the provided changed-file list/diff. Applying or committing only the shown diff will break the build.  
   **Suggested fix:** include `src/utils/uiPrefs.ts` in the change set, or move the preference helper into an already tracked file.
