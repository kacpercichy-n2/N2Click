**Findings**

1. **P1 — [src/components/ChatMock.tsx](/Users/kacpercichyn2/Documents/N2click/src/components/ChatMock.tsx:1), [src/components/FilterPanel.tsx](/Users/kacpercichyn2/Documents/N2click/src/components/FilterPanel.tsx:1) — new source files are untracked**
   Issue: `git status` shows both files as `??`, while tracked pages import them. A commit or patch made from the tracked diff/listed files will fail to compile with unresolved imports.
   Suggested fix: add both component files to the change set/commit and rerun `npx tsc --noEmit` from a clean checkout.

2. **P2 — [src/pages/DashboardPage.tsx](/Users/kacpercichyn2/Documents/N2click/src/pages/DashboardPage.tsx:60) — zero-availability overload renders as 0%, not danger**
   Issue: `over` is only true when `available > 0 && booked > available`. If a person has 0 available hours today, such as no workday or `workDays: []`, but has booked work, the donut shows `0%` and non-danger styling.
   Suggested fix: treat `booked > available` as overloaded, and for `available <= 0 && booked > 0` render a full danger ring or explicit “brak dostępności” state.

3. **P2 — [src/pages/TimelinePage.tsx](/Users/kacpercichyn2/Documents/N2click/src/pages/TimelinePage.tsx:355) — people-mode conflict markers are task-wide, not person-specific**
   Issue: each person row uses `conflictDatesForTask(state, t.id)`, so a bar for Marek can show overload markers caused only by Ola on the same task. In people mode, rows are per person, so this misattributes warnings.
   Suggested fix: compute conflicts for `(taskId, personId)` only, e.g. add `conflictDatesForTaskPerson` and use that in `peopleView`.

I did not rerun the full test suite; this was a static review of the working tree and diff.
