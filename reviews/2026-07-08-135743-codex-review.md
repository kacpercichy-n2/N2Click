1. **File:** `src/components/TaskModal.tsx:293-298`, `src/components/TaskModal.tsx:428-434`, `src/store/AppStore.tsx:273-307`  
   **Severity:** P1  
   **Issue:** Multiple timed blocks for the same `taskId/personId/date` are collapsed by the task editor. The allocation map is keyed only by `personId|date`, so opening/saving a task with two same-day blocks overwrites all but one and can delete planned hours/times. This is easy to create because WeekView’s insert form defaults to the same task.  
   **Suggested fix:** Either prevent inserting a second block for the same task/person/date, or change TaskModal/SAVE_TASK to preserve multiple workload entries per cell instead of flattening them to one allocation.

2. **File:** `src/components/WeekView.tsx:28-37`, `src/store/AppStore.tsx:29-38`  
   **Severity:** P1  
   **Issue:** The changed tracked files import `../utils/time`, but `src/utils/time.ts` is currently untracked, along with the new tests/config. If only the listed/tracked changes are committed, the app will fail typecheck/build.  
   **Suggested fix:** Add the new files to the commit: `src/utils/time.ts`, `src/utils/time.test.ts`, `src/store/blockActions.test.ts`, `vitest.config.ts`, and any intended handoff docs.

3. **File:** `src/store/AppStore.tsx:480-522`  
   **Severity:** P2  
   **Issue:** `INSERT_BLOCK` can leave same-person overlaps near the end of the day. Example: inserting a 2h block after a 22:00-23:00 block clamps the new start backward, then clamps pushed blocks backward too, so the “ripple” can still overlap.  
   **Suggested fix:** Simulate the ripple and reject unchanged if the chain cannot fit before 24:00, or cap the insert duration to actual available tail space before dispatching.

4. **File:** `src/components/AllocationGrid.tsx:146-151`, `src/components/WeekView.tsx:315-325`, `src/store/AppStore.tsx:281-305`, `src/store/AppStore.tsx:474-492`  
   **Severity:** P2  
   **Issue:** Changing `step` to `0.25` does not enforce 15-minute increments. Users can type values like `1.3` or `0.1`; SAVE_TASK/INSERT_BLOCK persist them, but `SET_BLOCK_TIME` later rejects those blocks because it requires multiples of `0.25`, making them effectively immovable in the timed grid.  
   **Suggested fix:** Validate or snap planned hours in the write paths (`SAVE_TASK`, `INSERT_BLOCK`) and disable/error in the UI when the value is not a multiple of `0.25` within `[0.25, 24]`.
