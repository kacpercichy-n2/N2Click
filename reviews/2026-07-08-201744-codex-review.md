1. **P1 — [src/pages/PeoplePage.tsx:22-35](/Users/kacpercichyn2/Documents/N2click/src/pages/PeoplePage.tsx:22), [src/store/AppStore.tsx:1304-1314](/Users/kacpercichyn2/Documents/N2click/src/store/AppStore.tsx:1304)**  
   **Issue:** Fresh setup can lock itself out. With zero people, the add-person form is allowed, but the default role is `pracownik`; `ADD_PERSON` preserves that and does not set `currentUserId`. After adding the first person, the login gate activates and the only user lacks `people.manage` / `admin.panel`, so no one can add/promote users without editing localStorage.  
   **Suggested fix:** Make the first created person an `administrator` in the reducer, or prevent saving a zero-admin people set. Also guard demoting/deleting the last administrator.

2. **P2 — [src/store/AppStore.tsx:1085-1092](/Users/kacpercichyn2/Documents/N2click/src/store/AppStore.tsx:1085)**  
   **Issue:** `DELETE_PERSON` removes the person but leaves `supervisorId` references pointing at the deleted id. That creates dangling org-chart state and broken/misleading profile forms.  
   **Suggested fix:** When deleting a person, map remaining people and clear `supervisorId` where it equals the deleted `personId`; add a reducer test.

3. **P2 — [src/components/WeekView.tsx:899-917](/Users/kacpercichyn2/Documents/N2click/src/components/WeekView.tsx:899), [src/components/WeekView.tsx:966-982](/Users/kacpercichyn2/Documents/N2click/src/components/WeekView.tsx:966)**  
   **Issue:** Users with only `blocks.editOwn` can right-click an own block, choose “Dodaj przed/po”, then select any task in the system. `INSERT_BLOCK` also auto-adds an assignment, so this bypasses the read-only TaskModal and lets non-task-managers allocate themselves to arbitrary tasks.  
   **Suggested fix:** Hide insert actions unless `tasks.manage`, or for `blocks.editOwn` restrict insertion to the current task / tasks already assigned to that person.

4. **P2 — [src/components/TaskModal.tsx:394-399](/Users/kacpercichyn2/Documents/N2click/src/components/TaskModal.tsx:394), [src/components/AllocationGrid.tsx:187-190](/Users/kacpercichyn2/Documents/N2click/src/components/AllocationGrid.tsx:187)**  
   **Issue:** “Wypełnij dni robocze” still means Mon-Fri and hardcoded `8`, ignoring the new per-person `workDays` and capacity. For seeded Ola `[1,2,3,4]`, it fills Friday anyway.  
   **Suggested fix:** Use `isPersonWorkday(state, personId, d)` and `personCapacity(state, personId)` / `availableHoursOnDate` for fill behavior and update the tooltip.

5. **P3 — [src/pages/WorkloadPage.tsx:287-289](/Users/kacpercichyn2/Documents/N2click/src/pages/WorkloadPage.tsx:287)**  
   **Issue:** If a person has `workDays: []` and assigned hours, `available` is `0` but `pct` renders as `0%`, hiding an impossible workload. Empty workdays are explicitly allowed.  
   **Suggested fix:** Treat `available === 0 && assigned > 0` as over-capacity in the summary display.

Tests not run; this was a static review.
