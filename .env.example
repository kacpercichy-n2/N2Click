# Skopiuj ten plik do .env.local i uzupełnij wartości ze swojego projektu Supabase.
# .env.local jest ignorowany przez git — nie commituj prawdziwych wartości.
# Używaj wyłącznie klucza publishable (sb_publishable_...), nigdy sekretnego.
#
# Build produkcyjny bez poprawnych wartości Supabase jest fail-closed (ekran
# błędu konfiguracji). Wizualny build QA w trybie lokalnym wymaga podwójnego
# opt-inu: VITE_AUTH_MODE=local ORAZ `vite build --mode local-qa` (w dev
# wystarcza sama zmienna). Sama zmienna w środowisku deployu nic nie otwiera.
VITE_SUPABASE_URL=https://twoj-projekt.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_twoj_klucz

# Dysk Google w module Content Plan (wybór mediów przez Google Picker).
# Obie wartości bierzesz z Google Cloud dla projektu N2:
#  - VITE_GOOGLE_CLIENT_ID: klient OAuth typu „Web application" o nazwie
#    „N2 Content Planner Web" (Credentials > OAuth 2.0 Client IDs). Adres
#    aplikacji musi być na liście Authorized JavaScript origins.
#  - VITE_GOOGLE_API_KEY: klucz API z tego samego projektu, ograniczony do
#    Google Picker API (Credentials > API keys).
# Bez tych zmiennych moduł Content Plan działa normalnie, wyłączone są tylko
# przyciski wyboru plików i folderu z Dysku (przycisk pokazuje powód blokady).
VITE_GOOGLE_CLIENT_ID=123456789012-przykladowy.apps.googleusercontent.com
VITE_GOOGLE_API_KEY=twoj_klucz_api_google

# GIF-y w czacie (picker KLIPY). Klucz wyrabiasz samodzielnie w Partner Panelu:
#  - Zaloguj się na https://partner.klipy.com i wejdź w API Keys.
#  - „Add Platform" > typ Web > podaj App URL wdrożonej aplikacji; panel wyda
#    klucz testowy dla tej platformy.
#  - Klucz testowy ma limit 100 zapytań na godzinę. Po sprawdzeniu integracji
#    złóż w panelu wniosek o dostęp produkcyjny (bezpłatny, bez limitu zapytań);
#    formularz pyta o kategorię aplikacji, szacowane MAU i nagranie ekranu.
# Klucz jest PUBLICZNY — jedzie segmentem ścieżki w adresie i ląduje w bundlu.
# Bez tej zmiennej czat działa normalnie — znika wyłącznie przycisk GIF przy
# kompozytorze (emoji, linki i wklejony adres GIF-a działają dalej).
VITE_KLIPY_API_KEY=
