# Agent context

Start with `CLAUDE.md`. For implementation and review, read only the prompt's
declared pages under `openwiki/n2hub/`, its expected touchpoints and direct
dependencies. `openwiki/quickstart.md` is a short routing page, not a requirement
to read every linked document.

Do not scan historic handoffs, generated wiki narratives or the whole repository
without a concrete dependency. Record every necessary context expansion.

OpenWiki generation is optional and local. The deterministic CI check validates
wiki links without calling an LLM or external API.
