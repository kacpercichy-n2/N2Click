# Raport workflow: 20260723-130447-n2hub-270-tab-badge-unread

## Wykonane

Funkcja NIE istniała w buildzie (brak jakichkolwiek śladów `tabBadge`/`useTabBadge`) — zaimplementowano ją w całości:

- **`src/utils/tabBadge.ts`** (nowy): czysta logika badge'a karty przeglądarki:
  - `unreadBadgeLabel(count)`: 0/niepoprawne → brak badge'a, 1–9 → cyfra, >9 → `9+`;
  - `titleWithBadge(baseTitle, count)`: np. `(3) N2Hub Planer`, przy 0 bazowy tytuł;
  - `unreadNotificationCountFor(notifications, personId)`: licznik z tych samych kryteriów co karta „Powiadomienia” na Panelu (`recipientId === zalogowany`, `readAt === ''`); brak osoby → 0;
  - `createTabBadgeApplier(host)`: czysta maszyna stanu apply/restore — zapamiętuje bazowy tytuł przy pierwszej aktywacji (prefiksy się nie piętrzą), pomija powtórzenia tej samej etykiety (10 → 11 zostaje `9+`, zero przerysowań = brak migotania), przy zeru przywraca tytuł i faviconę;
  - cienka, nietestowana warstwa DOM/canvas: `drawBadgeFaviconDataUrl` (canvas 32×32, czerwona kropka z liczbą w prawym górnym rogu, czytelna przy 16×16) i `createDomTabBadgeHost` (zarządzanie `link[rel~="icon"]`).
- **`src/utils/useTabBadge.ts`** (nowy): hook — `useEffect` kluczowany etykietą (nie surowym licznikiem), applier dodatkowo deduplikuje; cleanup przy odmontowaniu przywraca oryginalny tytuł i faviconę.
- **`src/App.tsx`**: wpięcie RAZ, wysoko w `App()` (przed bramkami logowania, bezwarunkowo — zgodnie z rules of hooks): `useTabBadge(unreadNotificationCountFor(state.notifications, currentUser?.id))`. Działa na każdej stronie; realtime-merge (`MERGE_CLOUD_NOTIFICATIONS` z 266/269) aktualizuje licznik także w tle — bez żadnego nowego pollingu. Wylogowanie → licznik 0 → przywrócenie karty.
- **`openwiki/n2hub/ui-navigation-and-onboarding.md`**: dopisano granicę badge'a karty (nowy moduł + punkt wpięcia).

Jak liczony jest licznik: `state.notifications.filter(n => n.recipientId === currentUserId && n.readAt === '').length` — dokładnie te same dane co selektor `unreadNotificationsForPerson` zasilający kartę „Powiadomienia”; funkcja jest osobną czystą implementacją w utils (konwencja: utils bez importów store), bez sortowania (niepotrzebne do zliczenia).

Odstępstwo od treści promptu: `index.html` NIE zawiera `<link rel="icon">` (aplikacja nie ma dziś favicony), więc nie ma bazowej grafiki do overlay'a. Host tworzy własny element `link` (usuwany przy liczniku 0) i rysuje ciemny kafelek w motywie aplikacji + czerwoną kropkę. Ścieżka overlay'a na bazowej faviconie jest jednak zaimplementowana (jednorazowe doładowanie `Image` + ponowny rysunek) i zadziała automatycznie, gdy favicona kiedyś dojdzie.

## Zmiany

- `src/utils/tabBadge.ts` (nowy)
- `src/utils/useTabBadge.ts` (nowy)
- `src/utils/tabBadge.test.ts` (nowy)
- `src/App.tsx` (import + 1 wywołanie hooka)
- `openwiki/n2hub/ui-navigation-and-onboarding.md` (nowa granica)

## Weryfikacja

- `npm test`: **65 plików / 1508 testów — wszystkie zielone** (w tym 17 nowych w `tabBadge.test.ts`: etykiety 0/1/9/10/99, tytuł, licznik własne-nieprzeczytane vs cudze/przeczytane, apply/restore, brak przerysowań przy tej samej etykiecie, przywracanie przy 0 i w dispose).
- `npm run build` (tsc --noEmit + vite build): **zielony** (ostrzeżenie o rozmiarze chunka >500 kB istniało wcześniej).
- Gate (`npm test && npm run build`): oczekuje na scheduler.

## Ryzyka / rzeczy do sprawdzenia

- Warstwa DOM/canvas (`drawBadgeFaviconDataUrl`, `createDomTabBadgeHost`) jest zgodnie z promptem cienka i bez testów jednostkowych (środowisko testowe to czysty node, bez jsdom); logika etykiet/tytułu/licznika/przywracania jest w pełni czysta i przetestowana. Wizualną czytelność kropki przy 16×16 warto potwierdzić okiem przy najbliższej weryfikacji przeglądarkowej.
- Nikt inny w kodzie nie modyfikuje `document.title` (sprawdzone grepem), więc zapamiętany bazowy tytuł jest stabilny.
- Zero zmian w bazie, RLS, syncu i reducerach; brak nowych zależności.

## Podpis schedulera

- Run: `20260723-130447-n2hub-270-tab-badge-unread`
- Prompt: `270-tab-badge-unread.md`
- Projekt: N2Hub (N2Click)
- Gałąź review: `review-integration`
- Baza: `65bdf1acf6aca10112c3ca9aff7065faef303264`


## Gate schedulera

- Gate (`npm test && npm run build`): zaliczony
- Baza: `65bdf1acf6aca10112c3ca9aff7065faef303264`
- Gałąź review: `review-integration`
- Run: `20260723-130447-n2hub-270-tab-badge-unread`

### Pliki zgłoszone do review

- `openwiki/n2hub/ui-navigation-and-onboarding.md`
- `src/App.tsx`
- `handoffs/scheduler-reviews/20260723-130447-n2hub-270-tab-badge-unread.md`
- `src/utils/tabBadge.test.ts`
- `src/utils/tabBadge.ts`
- `src/utils/useTabBadge.ts`
