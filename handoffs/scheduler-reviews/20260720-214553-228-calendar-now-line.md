# Raport workflow: 20260720-214553-228-calendar-now-line

## Wykonane

Analiza wstępna: funkcja nie istniała w bieżącym buildzie (brak wskaźnika
„teraz” i plakietki daty/zegara w `WeekView.tsx`/`styles.css`), więc zadanie
zrealizowałem.

1. **Linia „teraz”** (`src/components/WeekView.tsx`): w `WeekView` dodałem stan
   `now` odświeżany przez `setInterval` co 30 s (interwał sprzątany w cleanupie
   `useEffect`). W kolumnie dzisiejszego dnia — tylko gdy dzisiaj mieści się
   w wyświetlanym tygodniu (`isTodayStr(d)`) — renderowany jest
   `div.week-now-line` z `top = (minutyTeraz / 60) * HOUR_PX`.
2. **Plakietka narożna**: `div.week-now-badge` w lewym dolnym rogu widoku
   `.week-cal` z dzisiejszą datą z dniem tygodnia po polsku
   (`format(now, 'EEEE, d MMMM', { locale: pl })`) i żywym zegarem `HH:mm`.
3. **Style** (`src/styles.css`): linia w kolorze `--n2-danger` z kropką po
   lewej, `z-index: 10` (nad statycznymi blokami, pod przeciąganym
   `.dragging` z z-index 30); plakietka z ciemnym tłem i monospace zegarem.
   Oba elementy mają `pointer-events: none`.

Zgodność z inwariantem 7 (stability-sensitive): zmiana czysto prezentacyjna —
zero zmian w handlerach pointerów, ścieżkach drag/resize, hit-testingu siatki
i cyklu życia dragu z zasobnika.

## Zmiany

- `src/components/WeekView.tsx` — stan zegara + linia „teraz” + plakietka.
- `src/styles.css` — style `.week-now-line` i `.week-now-badge`.

## Weryfikacja

- `npm test`: **PASS** — 34 pliki / **960 testów** zielonych (prompt wspominał
  933 — licznik urósł we wcześniejszych etapach gałęzi integracyjnej).
- `npm run build`: **PASS**.
- `node scripts/browser-check-bin-drag.mjs chromium` (dev server na :5173
  w trybie lokalnym, `VITE_SUPABASE_URL='' VITE_SUPABASE_PUBLISHABLE_KEY=''`):
  **VERDICT: PASS** — zero błędów konsoli/strony, drag z zasobnika, modal po
  dropie i cleanup ghosta bez regresji.
- Dodatkowy smoke-check Playwright na `/calendar`: `.week-now-line` obecna,
  `style.top` = 1832.6px dokładnie równe `(minutyTeraz/60)*84` dla 21:49,
  `pointer-events: none` potwierdzone w computed style; plakietka renderuje
  „poniedziałek, 20 lipca” + „21:49”.

## Ryzyka / rzeczy do sprawdzenia

- Worktree schedulera nie miał pakietu `playwright` w `node_modules` (nie jest
  zależnością projektu); na potrzeby browser-checka podlinkowałem symlinkami
  `playwright` i `playwright-core` z głównego checkoutu repo. Symlinki żyją
  tylko w gitignorowanym `node_modules` jednorazowego worktree — bez wpływu na
  commit i gate'y.
- Odświeżanie co 30 s: linia/zegar mogą pokazywać poprzednią minutę maksymalnie
  przez ~30 s — świadomy kompromis (prompt dopuszczał „co minutę”).
- Wiki: **wiki unchanged** — `scheduling-and-calendar.md` opisuje granice
  i inwarianty interakcji; czysto prezentacyjny wskaźnik żadnej z nich nie
  zmienia.

## Podpis schedulera

- Run: `20260720-214553-228-calendar-now-line`
- Prompt: `228-calendar-now-line.md`
- Gałąź review: `review-integration`
- Baza: `80b09d3428fd1f4314bf85bf1759622669f032d2`


## Gate schedulera

- `npm test`: zaliczone
- `npm run build`: zaliczone
- Baza: `80b09d3428fd1f4314bf85bf1759622669f032d2`
- Gałąź review: `review-integration`
- Run: `20260720-214553-228-calendar-now-line`

### Pliki zgłoszone do review

- `reviews/screenshots-20260709-codex/chromium-01-before-drag.png`
- `reviews/screenshots-20260709-codex/chromium-02-during-drag.png`
- `reviews/screenshots-20260709-codex/chromium-03-after-drop.png`
- `src/components/WeekView.tsx`
- `src/styles.css`
- `handoffs/scheduler-reviews/20260720-214553-228-calendar-now-line.md`
