# Raport workflow: 20260721-154537-244-recurring-tasks

## Wykonane

Zadania cykliczne w kalendarzu — pełny przepływ tier (`architect → developer ×2 → reviewer`), funkcji nie było wcześniej w buildzie (weryfikacja grep: brak modelu/utila/UI cykliczności).

- **Model.** `Task.recurrence?: TaskRecurrence` osadzone na zadaniu (bez nowej encji): `{ daysOfWeek (ISO 1–7), startMinutes, durationMinutes (siatka 15 min), until?, overrides? }` w formie kanonicznej. Kotwicą jest `task.startDate`; szkice (drafty) nie mogą mieć reguły.
- **Util.** Nowy czysty `src/utils/recurrence.ts` (RRULE-lite, bez zależności, reużywalny dla przyszłych „wydarzeń”): `normalizeRecurrenceRule`, `normalizeRecurrence`, `isOccurrenceDate`, `expandOccurrences` — rozwijanie tylko dla widocznego zakresu (defensywny limit 400 dni), zero materializacji wierszy.
- **Edycja jednego vs wszystkich.** `SET_RECURRENCE_OVERRIDE` = „Edytuj to wystąpienie” (override per data: przesunięcie godziny/czasu trwania albo `skip`, plus przywrócenie); `SET_TASK_RECURRENCE` = „Edytuj wszystkie” (zmiana reguły bazowej z re-kanonizacją przeżywających override'ów). Każde odrzucenie zwraca tę samą referencję stanu (invariant 6). Zmiana `startDate` w `SAVE_TASK` re-kotwiczy regułę.
- **UI.** `TaskModal`: sekcja „Cykliczność” (chipy dni tygodnia, „Początek”, „Czas trwania”, „Do dnia (opcjonalnie)”, lista override'ów z „Przywróć zgodnie z regułą”), poza auto-save — jawny dispatch. `WeekView`: wystąpienia jako czysto prezentacyjny overlay (`.week-recur-block`, kreskowany/paskowany, ⟳), renderowany pod prawdziwymi blokami, z menu „Edytuj to wystąpienie” / „Edytuj wszystkie” / „Pomiń ten dzień”; menu pustego slotu (prompt 236) dostało tylko guard `.closest('.week-recur-block')`. `MonthView`: znacznik ⟳ per dzień. **Ścieżki pointer/drag i hit-test nietknięte (invariant 7)** — wystąpienia nie mają handlerów drag; nie wpływają na sumy godzin, przeciążenie ani kolizje.
- **Chmura + persist.** Kolumna `tasks.recurrence jsonb` — migracja `supabase/migrations/20260721170000_task_recurrence.sql` (`add column if not exists`, forward-only, idempotentna, zarejestrowana w `migrations.test.ts`; `EXPECTED_POLICIES` bez zmian — RLS dziedziczone z `tasks`). Mapowanie `cloudMirror.ts` (zapis) + `plannerData.ts` (hydratacja przez `normalizeRecurrence`, tylko opublikowane) + zachowanie referencji w MERGE_CLOUD (cloud-authoritative). `DATA_VERSION` zostaje 7; naprawa w `storage.ts` (`normalizeTaskMeta`) jest addytywna i idempotentna. **Migracja tylko w plikach — nie zastosowana na hostowanym Supabase** (konwencja jak w runie 243).
- **Review.** Reviewer: `approved`; jedyny should-fix (walidacja off-grid/NaN „Początku” w TaskModal) i nit (kolejność `preventDefault` vs gate uprawnień w `openRecurMenu`) zostały naprawione w tej samej sesji i ponownie zweryfikowane.
- **Wiki: updated** — `openwiki/n2hub/scheduling-and-calendar.md`, `state-and-persistence.md`, `cloud-database.md` (reviewer potwierdził zgodność z kodem).

## Zmiany

- `src/types.ts`, `src/utils/recurrence.ts` (nowy) + testy, `src/store/AppStore.tsx`, `src/store/selectors.ts`, `src/store/storage.ts`, `src/supabase/cloudMirror.ts`, `src/supabase/plannerData.ts`, `src/supabase/migrations.test.ts`, `supabase/migrations/20260721170000_task_recurrence.sql` (nowy), `src/components/TaskModal.tsx`, `src/components/WeekView.tsx`, `src/components/MonthView.tsx`, `src/styles.css`, wiki (3 strony), pakiety architekta w `handoffs/packages/`.

## Weryfikacja

- Testy focused: `recurrence.test.ts` (25), `recurrenceActions.test.ts` (21) + rozszerzenia `storage/cloudMirror/plannerData/cloudMerge/migrations` — zielone.
- `npm test`: **1244 passed (46 plików)** — zielony.
- `npm run build`: zielony (tsc + vite; ostrzeżenie o chunku >500 kB istniało wcześniej, bez zmian).
- `scripts/browser-check-bin-drag.mjs`: **PASS** (uruchomiony ponownie po poprawce review); dodatkowo `browser-check-placement.mjs` i `browser-check-bin-split.mjs`: **PASS**.

## Ryzyka / rzeczy do sprawdzenia

- Migracja `20260721170000_task_recurrence.sql` czeka na zastosowanie na hostowanym Supabase (za zgodą usera, przez MCP `apply_migration`); do tego czasu zapis lustrzany kolumny `recurrence` w chmurze nie zadziała na produkcyjnej bazie.
- Drag wystąpień cyklicznych świadomie pominięty (invariant 7) — przesunięcie pojedynczego wystąpienia wyłącznie przez menu kontekstowe/edycję; ewentualny drag-do-override'a to osobny, przyszły prompt.
- MonthView pokazuje tylko znacznik ⟳ (miesiąc nie renderuje bloków także dla zwykłych wpisów) — świadome odstępstwo od litery promptu, spójne z istniejącym widokiem.
- Playwright do browser-checków był podlinkowany tymczasowo z cache npx (bez zmian w `package.json`).

## Podpis schedulera

- Run: `20260721-154537-244-recurring-tasks`
- Prompt: `244-recurring-tasks.md`
- Gałąź review: `review-integration`
- Baza: `f1cfd2988100b4b4633cafb7296c34f4f3d7fbad`


## Gate schedulera

- `npm test`: zaliczone
- `npm run build`: zaliczone
- Baza: `f1cfd2988100b4b4633cafb7296c34f4f3d7fbad`
- Gałąź review: `review-integration`
- Run: `20260721-154537-244-recurring-tasks`

### Pliki zgłoszone do review

- `handoffs/RUN-STATE.md`
- `openwiki/n2hub/cloud-database.md`
- `openwiki/n2hub/scheduling-and-calendar.md`
- `openwiki/n2hub/state-and-persistence.md`
- `src/components/MonthView.tsx`
- `src/components/TaskModal.tsx`
- `src/components/WeekView.tsx`
- `src/store/AppStore.tsx`
- `src/store/cloudMerge.test.ts`
- `src/store/selectors.ts`
- `src/store/storage.test.ts`
- `src/store/storage.ts`
- `src/styles.css`
- `src/supabase/cloudMirror.test.ts`
- `src/supabase/cloudMirror.ts`
- `src/supabase/migrations.test.ts`
- `src/supabase/plannerData.test.ts`
- `src/supabase/plannerData.ts`
- `src/types.ts`
- `handoffs/packages/244-recurrence-core.md`
- `handoffs/packages/244-recurrence-ui.md`
- `handoffs/scheduler-reviews/20260721-154537-244-recurring-tasks.md`
- `src/store/recurrenceActions.test.ts`
- `src/utils/recurrence.test.ts`
- `src/utils/recurrence.ts`
- `supabase/migrations/20260721170000_task_recurrence.sql`
