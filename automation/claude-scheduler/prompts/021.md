lets start a tier workflow

MVP bundle: mini profile hover card.

Add a lightweight person profile tooltip/hover card around the existing person UI.

Scope:
- Extend or wrap `PersonChip` with a hover/focus card.
- Show:
  - avatar
  - name
  - role/job title
  - department
  - email/phone if present and permission allows
  - today's availability and planned hours
- Use existing `personColor`, workload selectors, and profile data.
- Ensure keyboard/focus accessibility, not only mouse hover.
- Keep the card compact and visually consistent.

Acceptance:
- Hover/focus over a person chip shows the mini profile.
- The card does not overflow on small screens.
- Sensitive contact details follow current permission rules if applicable.
- No regressions in existing PersonChip layout.
- Run `npm test` and `npm run build`.
- Update docs/handoff notes.
- Commit only this bundle to the review branch with a clear message.

push it to git review
