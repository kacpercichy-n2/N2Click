lets start a tier workflow

Release-hardening bundle 9: make critical browser checks and documentation reliable.

Context: unit tests are useful but run only in Node, the browser regression harness is outside normal verification, and core docs are stale relative to storage v6/current behavior. The audit found reproducible browser defects despite green unit tests.

Current baseline — verify and extend, do not rebuild:
- Invalid-date recovery, multi-block unchanged save, stable status semantics, resize→bin conservation, and the stuck bin-drag lifecycle are already repaired on main.
- `scripts/browser-check-bin-drag.mjs` already includes real Chromium/WebKit drag plus `window-fallback`, collision, separator, invalid-target, ghost-cleanup, heartbeat, and narrow-layout probes. Keep these cases and their failure conditions intact.
- Do not restore element pointer capture for bin cards, width-division day targeting, or state-only final pointer projections while reorganizing tests.

Scope:
- Add a stable npm script that orchestrates the existing Chromium/WebKit regression scripts and make it part of release verification documented for scheduler/CI use. Reuse the current scripts instead of rewriting their scenarios. Keep it deterministic and avoid requiring a manually running dev server when practical.
- Treat already-repaired flows as regression coverage, not implementation tasks. Add missing end-to-end coverage only for functionality actually delivered by bundles 016–020: oversized-bin partial recovery, remaining automatic-placement gaps, persistence failure signal, and zero availability.
- Add minimal accessibility checks for keyboard-visible focus, dialogs/error recovery, and a non-pointer path for any critical remediation UI added by prior bundles.
- Refresh CLAUDE.md, RUN-STATE, and workflow docs from the actual code. Remove stale claims such as old storage versions or obsolete role assumptions; clearly state that client-side auth/localStorage is prototype-only.
- Do not introduce a broad new product feature in this run.

Acceptance:
- One documented command runs the production build plus meaningful browser regression coverage.
- The critical audit repro list has an automated or explicit documented manual check.
- Documentation accurately describes current code and remaining production blockers.
- Run all fresh direct checks and commit only this quality-hardening bundle.

push it to git review
