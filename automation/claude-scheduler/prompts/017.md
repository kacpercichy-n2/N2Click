lets start a tier workflow

Release-hardening bundle 5: centralize scheduling and date invariants.

Context: timeline resizing bypasses the 92-day task limit, placement helpers can clamp a block backwards onto existing work, and child dates can drift outside their project. UI-only checks are insufficient.

Scope:
- Create shared domain helpers for valid task/project periods, 92-day maximum, parent-project containment policy, and collision-safe placement.
- Enforce those invariants in every reducer/action path, including TaskModal, timeline resize, drag/drop, right-click insertion, reassignment, split, migration/restacking, and imported state normalization.
- Replace clamp-back overlap behavior with a real free-slot search or explicit rejection. Never create an overlapping block merely to stay within midnight.
- When an action is rejected, retain the prior state and give the UI a persistent Polish reason where the existing interaction can display one.
- Update tests that currently bless end-of-day overlap; the invariant is no silent collision creation.

Acceptance:
- A timeline resize cannot create a task longer than 92 days or invalid period.
- New/inserted/moved blocks never overlap their owner's existing timed blocks; impossible placement is rejected clearly.
- Parent/child period policy is deterministic and tested.
- Add focused reducer/time utility/browser coverage for all reported bypasses.
- Run fresh direct tsc, vitest, production build, Chromium and WebKit checks; update docs/handoff notes.

push it to git review
