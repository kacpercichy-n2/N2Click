lets start a tier workflow

MVP bundle: sales process and permission split.

Separate sales management from operational planning.

Scope:
- Add or refine sales pipeline fields using the formal project status from the project-card bundle if it exists.
- Sales stages: lead, brief, oferta, negocjacje, wygrany, przegrany.
- Add `HAND_OFF_PROJECT` action:
  - sales owner assigns PM
  - project moves toward realization only when required formal data is ready if those checks exist
  - activity entry records the handoff
- Permission split:
  - Handlowiec can manage sales-facing project data.
  - Handlowiec should not get broad operational planning permission unless explicitly allowed.
  - PM/admin keep operational planning controls.
- Update navigation/action visibility according to central permissions.

Acceptance:
- Sales users can manage leads/sales stages but cannot perform restricted planning actions.
- Handoff assigns PM and logs activity.
- Existing role behavior for PM/admin/pracownik does not regress.
- Add permission matrix tests if not already present.
- Run `npm test` and `npm run build`.
- Update docs/handoff notes.
- Commit only this bundle to the review branch with a clear message.

push it to git review
