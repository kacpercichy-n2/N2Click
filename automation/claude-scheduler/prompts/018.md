lets start a tier workflow

Release-hardening bundle 6: make local persistence honest and reduce same-browser tab loss.

Context: localStorage writes are swallowed while the UI reports success, and independent tabs can overwrite each other wholesale. This cannot solve multi-user/server synchronization, but the prototype must not falsely claim a successful save.

Scope:
- Make storage writes return an explicit success/failure result. Detect quota/private-mode/serialization failures and surface a durable Polish error state with recovery guidance.
- Connect save-status UI to actual write outcomes; it must never display "Zapisano" after a failed write.
- Add a small persisted revision or timestamp protocol plus a storage-event listener. On external changes, refresh safely when local state is clean; when local changes conflict, show an explicit conflict/reload choice instead of silently overwriting.
- Keep all data local in this bundle. Do not claim this creates real collaboration, authorization, backups, or a production backend.
- Add tests with failing localStorage and revision/conflict cases; add a browser/manual verification path for two tabs if the harness can support it.

Acceptance:
- Failed persistence is visible and no false saved confirmation appears.
- An external tab update is detected; conflicting local edits are not silently replaced.
- Existing storage migrations and normal reload behavior remain compatible.
- Run fresh direct tsc, vitest, production build, and relevant browser checks; update docs/handoff notes.

push it to git review
