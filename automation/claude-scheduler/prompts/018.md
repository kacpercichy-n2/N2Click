lets start a tier workflow

MVP bundle: absences and availability.

Add vacation/absence support and wire it into availability calculations.

Scope:
- Add `Absence`: personId, start date, end date, type, optional note.
- Absence types should cover urlop, chorobowe, delegacja, inne.
- UI:
  - Person profile: manage absences.
  - Workload page: available hours reflect absences.
  - Calendar/timeline: show absence markers or unavailable state.
- Availability:
  - Update `availableHoursOnDate` or the current equivalent selector to account for absences.
  - Absence should reduce available capacity to zero or partial capacity only if you deliberately implement partial absences; keep it simple if unclear.
- Preserve current workDays and daily capacity behavior.

Acceptance:
- Adding an absence changes workload availability and overload display.
- Existing workload totals remain stable except for availability math.
- Absences persist through reload.
- Add selector/storage tests for absence overlap and availability.
- Run `npm test` and `npm run build`.
- Update docs/handoff notes.
- Commit only this bundle to the review branch with a clear message.

push it to git review
