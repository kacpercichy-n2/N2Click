lets start a tier workflow

MVP bundle: later MVP foundations.

Prepare small, low-risk foundations for future project templates, "find free slot", and reporting without overbuilding them.

Scope:
- Project templates:
  - Add a minimal data model or design note only if implementation is small and clearly reusable.
  - Preferred MVP behavior: create a project from a template that can prefill tasks/checklists/stages.
- Find free slot:
  - Add pure helper functions based on existing `availableHoursOnDate`, day blocks, collision checks, deadlines, and 15-minute slots.
  - Do not add AI scheduling or complex optimization yet.
- Reports:
  - Add pure selectors for future time/cost/profitability reports based on estimate, planned, actual time, and budget if those fields exist.
  - If actual time or budget does not exist yet, write the selectors defensively and document blocked dependencies.
- Keep UI minimal; this bundle can be mostly pure helpers, tests, and docs if that is the safest move.

Acceptance:
- No broad UI rewrite.
- Pure helpers/selectors are tested.
- Dependencies on earlier MVP bundles are documented honestly.
- Existing app behavior does not change unless the feature is actually wired safely.
- Run `npm test` and `npm run build`.
- Update docs/handoff notes.
- Commit only this bundle to the review branch with a clear message.

push it to git review
