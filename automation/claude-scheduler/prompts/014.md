lets start a tier workflow

MVP bundle: project card core business fields.

Extend project metadata without changing the existing operational status pipeline.

Scope:
- Add project fields:
  - `pmId`
  - `salesOwnerId`
  - optional `teamIds`
  - optional project budget field
  - separate `formalStatus`
- `formalStatus` must not reuse or replace the current operational `Status` pipeline.
- Suggested formal statuses: lead, oferta, umowa podpisana, gotowe do realizacji, w realizacji, zamknięte, przegrane. Verify the best Polish labels and keep code literals stable.
- UI in `ProjectDetailPage` following existing form styling.
- Budget visibility must respect permissions. Do not expose budget to regular employees.
- Project list/detail should show the most useful compact metadata without clutter.

Acceptance:
- Existing projects migrate with safe defaults.
- Operational status and formal status are visibly separate.
- Permission checks protect budget display/editing.
- Add storage/selector tests for migration and visibility if a helper is added.
- Run `npm test` and `npm run build`.
- Update docs/handoff notes.
- Commit only this bundle to the review branch with a clear message.

push it to git review
