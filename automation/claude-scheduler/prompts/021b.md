# Close focused browser and accessibility coverage gaps

## Risk and routing
- Risk: medium
- Route: architect → developer → reviewer
- Codex review: conditional — run if production code changes or reviewer uncertainty remains

## Wiki context
- `openwiki/n2hub/testing-and-automation.md`
- `openwiki/n2hub/scheduling-and-calendar.md`
- `openwiki/n2hub/state-and-persistence.md`

## Expected touchpoints
- `scripts/browser-check-*.mjs`

## Invariants
- Existing bin pointer cleanup, rendered-column targeting and state projections remain unchanged.
- Test changes must assert user-visible behavior, not implementation-only state.
- Previously repaired scenarios are extended, not rewritten.

## Scope
- Add only missing regression cases for bundles 016–020: partial-bin recovery, automatic-placement gaps, persistence failure signal and zero availability.
- Add minimal keyboard-visible focus, dialog/error recovery and non-pointer remediation checks where those flows already exist.

## Out of scope
- Product feature work, general accessibility redesign and replacement of existing browser harnesses.

## Acceptance
- Every named audit reproduction has an automated assertion or a precise manual fallback note.
- New checks fail when the covered regression is deliberately reintroduced.

## Verification
- Worker: run only newly changed scripts in Chromium while iterating.
- Browser: run changed scripts in Chromium and WebKit once after focused checks are stable.
- Scheduler: owns the single final `npm test && npm run build` gate.
