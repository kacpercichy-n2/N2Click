okay lets do a tier workflow. know lets backoff from upgradeing our features and do a review on our current files, check our md files and update them with current state of the project, delete information/tasks that is no longer important or got implemented, do code review looking for loop holes, bad code behaviour or other stuff. This run is mostly reorganization, matinance, and validation.

The dragging tasks from the bin function still doesnt work, it still freezes when we drag a task from the bin onto the calendar. Run a Codex and Opus agent in the browser to check it out and screenshot what is happening.
