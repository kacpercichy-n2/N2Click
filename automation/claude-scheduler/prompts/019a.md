Use the tier workflow.

Release-hardening bundle 7a: validate persistent reducer commands without changing navigation or activity behavior.

Scope only this run:
- Audit reducer actions that create or mutate tasks, projects, milestones, statuses, people, or the current user.
- Introduce or complete one shared validation boundary for stale/invalid IDs, empty required titles, malformed dates, estimates, and broken status/project references.
- Rejected commands must preserve the previous state reference and have focused reducer tests.
- Preserve all existing valid SAVE_TASK workload reconciliation, bin behavior, date guards, and status semantics. Do not change dirty-navigation prompts, login/audit events, impersonation attribution, or browser routing in this run.

Acceptance:
- Demonstrated invalid/stale payloads cannot persist malformed entities.
- Existing valid command tests remain green unchanged; do not make validation so broad that valid legacy edit payloads no-op.
- Add only focused reducer/unit tests needed for this boundary.
- Run direct typecheck, vitest, and production build; update the handoff/run notes.

Push only the current review branch when the work is green.
