Use the tier workflow.

Release-hardening bundle 7c: make local activity attribution honest.

## Wiki context

- `openwiki/n2hub/state-and-persistence.md`
- `openwiki/n2hub/ui-navigation-and-onboarding.md`

## Expected touchpoints

- `src/store/AppStore.tsx`, `src/store/selectors.ts`, `src/store/permissions.ts`, `src/store/*.test.ts`

Scope only this run:
- Record both the acting identity and the real administrator identity in local activity events created while impersonating.
- Add focused local activity events for currently omitted sensitive actions where the existing model supports them: role/person changes, status administration, deletions, password changes, login/logout, and impersonation start/end.
- Keep the local audit trail explicitly described as mutable and non-security-grade until a backend exists.
- Do not alter reducer command validation, navigation guards, authorization policy, or storage conflict handling in this run.

Acceptance:
- An activity entry created during impersonation identifies both the admin and the acted-as user.
- Covered sensitive actions have clear, non-duplicated local activity entries.
- Add focused reducer tests; run direct typecheck, vitest, and production build; update docs/handoff notes with the backend audit-trail limitation.

Push only the current review branch when the work is green.
