lets start a tier workflow

MVP bundle: full client card.

Build the first full client detail page and enrich the client model.

Scope:
- Add `/clients/:id`.
- Add client fields:
  - NIP
  - address
  - industry
  - cooperation status
  - billing terms
  - responsible person
- Add `ClientContact`: first name, last name, email, phone, role.
- Client page tabs/sections:
  - informacje
  - projekty
  - taski
  - dokumenty
  - notatki
  - kontakty
  - linki
  - aktywność
- Reuse existing comments/activity patterns where practical.
- Link from client names in project views to the client card.

Acceptance:
- Existing clients migrate safely.
- Contacts can be added/edited/deleted.
- Client card shows related projects and tasks using existing selectors.
- Route reload works directly at `/clients/:id`.
- Add storage/reducer tests for client fields and contacts.
- Run `npm test` and `npm run build`.
- Update docs/handoff notes.
- Commit only this bundle to the review branch with a clear message.

push it to git review
