lets start a tier workflow

Release-hardening bundle 4: make bin hours recoverable and partially schedulable.

Context: merged bin rows can exceed 24 hours, the calendar correctly refuses them, but the UI offers no real split path. Current split behavior aggregates remaining pieces back into one bin row, contradicting the UI and blocking normal planning.

Current baseline — already fixed, do not redo or weaken:
- `main` must contain commits `86aa3e6` and `11f1dea`. If it does not, stop and report a stale branch instead of editing.
- Bin→calendar drag is window-owned and intentionally does not depend on element pointer capture. It has guaranteed cleanup for pointerup/mouseup/cancel/blur/Escape/visibility loss, synchronous drag refs, rendered-day-column hit testing, invalid-target feedback, and scrollbar exclusion.
- `scripts/browser-check-bin-drag.mjs` covers free, window-fallback, collision, separator, invalid, and narrow Chromium/WebKit scenarios. Extend these checks; never replace them with a simpler happy-path-only drag test.
- This bundle is not another request to fix the stuck ghost or resize→bin race. Those problems are complete; preserve their behavior byte-for-byte unless a new failing regression proves otherwise.

Scope:
- Preserve the established single bin row per `(taskId, personId)` invariant unless a human explicitly approves a data-model change. Partial scheduling does not require duplicate bin rows: atomically decrement the source row by a user-selected 0.25h-aligned amount and create one dated row for that amount.
- Provide an accessible `Zaplanuj część` / split-amount UI on the bin card, including oversized rows. The chosen part must be between 0.25h and 24h, fit the day, and reuse the existing collision/period guards.
- Support repeated partial scheduling over several days while the unscheduled remainder keeps the original bin-row identity, ordering, and exact remaining hours. Delete the bin row only at zero.
- Do not redesign dated-block split semantics in this bundle; that is separate product behavior and is not required to recover oversized bin work.
- Preserve total planned hours and task estimates. Never require deleting hours or unassigning a person to recover from a large bin entry.

Acceptance:
- A 30-hour bin item can be split and scheduled over multiple days without loss.
- The scheduled part is visible on the calendar; the remaining amount stays as the same bin row and total hours remain conserved through reload.
- Calendar refusal messages exactly match available remediation actions.
- Add reducer/selectors and browser tests for 30-hour, partial-schedule, and split/reload paths.
- Every existing bin drag lifecycle scenario remains green in Chromium and WebKit, including `window-fallback`, invalid target, collision, separator, Escape, and narrow layout.
- Run fresh direct tsc, vitest, production build, Chromium and WebKit checks; update docs/handoff notes.

push it to git review
