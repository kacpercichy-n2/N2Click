lets start a tier workflow

Release-hardening bundle 7: harden command validation, dirty-navigation protection, and local audit attribution.

Context: reducer actions trust many stale/invalid IDs and values, browser Back discards dirty task/project edits, and activity does not record the real administrator during impersonation. Client-side permission checks remain cosmetic until a backend exists; do not present this work as production security.

Scope:
- Route all command payloads through shared validation for required IDs, titles, dates, estimates, status/project references, milestones, and current-user changes. Invalid commands must no-op with a testable result rather than corrupt state.
- Add navigation blocking/confirmation for dirty TaskModal and ProjectDetailPage on browser Back/forward and route transitions, not only custom buttons.
- Record both acting and real actor identity in local activity events when impersonating. Add activity events for sensitive local actions currently omitted where feasible: role/person changes, status administration, deletions, password changes, login/logout, and impersonation boundaries.
- Keep delete history limitations explicit; do not claim local activity is tamper-resistant.

Acceptance:
- Invalid/stale action payloads cannot persist broken entities.
- Browser navigation cannot silently discard a dirty task/project edit.
- Impersonated local activity identifies both the administrator and the acted-as user.
- Add reducer/navigation tests and a focused browser check.
- Run fresh direct tsc, vitest, production build; update docs/handoff notes with the remaining backend audit-trail requirement.

push it to git review
