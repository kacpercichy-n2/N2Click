lets start a tier workflow

MVP bundle: team monitor timeline.

Add a read-only daily team timeline for office monitor/kiosk use.

Scope:
- Add a new view separate from the existing project `TimelinePage`.
- Rows: people.
- Columns: today's hours.
- Blocks: today's planned work blocks.
- Add a current-time line that auto-updates every minute.
- Add kiosk/monitor mode:
  - full screen / no sidebar
  - date and current time visible
  - auto-refresh or periodic state refresh from local storage
  - filters for department, project, and client
  - no editing interactions
- Keep this view read-only.

Acceptance:
- Timeline renders today's plan by person.
- Current-time line moves without manual reload.
- Kiosk mode hides editing/navigation chrome.
- Filters work and do not mutate data.
- Add selector tests for today's blocks by person if needed.
- Run `npm test` and `npm run build`.
- Update docs/handoff notes.
- Commit only this bundle to the review branch with a clear message.

push it to git review
