lets start a tier workflow

Release-hardening bundle 8: correct availability and person-specific conflict signals.

## Wiki context

- `openwiki/n2hub/scheduling-and-calendar.md`
- `openwiki/n2hub/state-and-persistence.md`

## Expected touchpoints

- `src/store/selectors.ts`, `src/pages/WorkloadPage.tsx`, `src/pages/TimelinePage.tsx`, `src/components/WeekView.tsx`, focused selector tests

Context: zero availability can display as safe 0%, non-working days compare against daily capacity instead of actual availability, and people timeline rows show conflicts caused by another person. These defects mislead scheduling decisions.

Scope:
- Define one authoritative availability calculation for workdays, zero-capacity people, and future absence-ready behavior. Use it everywhere overload/percentage is shown.
- A booked day with zero available hours must be an explicit danger state, never 0% safe.
- Update dashboard, workload, calendar/timeline, and relevant selectors to compare booked hours against actual availability for that person/date.
- Make timeline people-mode conflict/overload markers strictly person-specific.
- Keep behavior for normal working days stable and avoid adding absence management in this bug-fix bundle.

Acceptance:
- 4h booked against 0h available is visibly overloaded/dangerous everywhere relevant.
- Non-working days and normal capacity days calculate consistently.
- A conflict for Ola does not appear as Marek's conflict in people mode.
- Add pure selector tests plus focused browser verification at desktop and narrow widths.
- Run fresh direct tsc, vitest, production build; update docs/handoff notes.

push it to git review
