lets start a tier workflow

MVP bundle: explicit task planning status.

Add a reusable planning-status concept for tasks based on existing planning data.

Scope:
- Create a selector/helper derived from task estimate, planned total, bin total, and remaining hours.
- Supported statuses:
  - `nie rozplanowano`
  - `częściowo`
  - `rozplanowano`
  - `przekroczono`
- Show the status in `TasksPage`, `TaskModal`, `ProjectDetailPage`, and the dashboard or `/my-work` if it exists.
- Make the display compact: badge/chip style consistent with current status badges.
- Add filters in `TasksPage` through `FilterPanel` if that page already uses it.

Acceptance:
- Planning status is fully derived, not stored redundantly.
- Edge cases are handled: null estimate, zero planned hours, bin-only hours, planned > estimate.
- Unit-test the pure helper/selector.
- Existing tests remain green.
- Update docs/handoff notes for the new derived status.
- Run `npm test` and `npm run build`.
- Commit only this bundle to the review branch with a clear message.

push it to git review
