lets start a tier workflow

MVP bundle: Moja praca.

Implement the first employee-focused work surface without touching unrelated areas.

Scope:
- Add a new `/my-work` page.
- Reuse existing selectors and data model wherever possible. Do not duplicate workload/task calculations.
- Sections on the page:
  - `Dzisiaj`: today's planned blocks/tasks for the current logged-in person.
  - `Zasobnik / nierozplanowane`: work that is in `BIN_DATE` / bin for that person, shown as task-level rows.
  - `Alerty`: overdue tasks, over-capacity days, blockers if already present in the model, and tasks with missing planning.
- For employee role users, redirect `/` to `/my-work` instead of the manager dashboard. PM/admin should keep the current dashboard behavior unless the existing permission model suggests a better split.
- Add sidebar navigation only if the current role should see it.
- Keep all visible UI strings Polish.

Acceptance:
- `/my-work` loads for a seeded employee and shows today's work, bin/unplanned work, and alerts.
- The page uses existing app styling and responsive behavior.
- No localStorage schema change unless unavoidable; if needed, add a migration and tests.
- Update `CLAUDE.md` or the appropriate handoff docs with the new route and behavior.
- Run `npm test` and `npm run build`.
- Commit only this bundle to the review branch with a clear message.

push it to git review
