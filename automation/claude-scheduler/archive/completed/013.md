lets start a tier workflow

Release-hardening bundle 1: eliminate invalid-date crashes and make bad persisted data recoverable.

Context: the latest comprehensive audit reproduced a blank screen when a project is saved with empty dates. Tasks also accept invalid dates and display NaN. This is a release blocker. Do not add new MVP features in this run.

Scope:
- Create one shared date/period validation utility used by project and task UI, reducer commands, and storage normalization. A valid date is a real calendar date in yyyy-MM-dd form, not merely a non-empty string.
- Project/task creation and edit must reject missing, invalid, reversed, or out-of-policy periods with a Polish inline explanation. Do not persist a command that fails validation.
- Make reducer paths defensive against invalid/stale actions and imported localStorage data. Normalize or safely reject corrupt task/project/milestone dates without corrupting valid data.
- Add a root React error boundary with a Polish recovery screen. It must preserve the ability to reset/export corrupted local data and return to a usable app rather than leaving a blank page.
- Cover the original browser reproduction: clear both project dates, submit, and verify the UI remains usable with no uncaught RangeError. Cover invalid persisted data and reducer actions in focused tests.

Acceptance:
- Empty/invalid dates cannot blank the app or persist through normal UI/reducer paths.
- The error boundary gives a recoverable UI for any unexpected render exception.
- Existing valid date workflows, migrations, and direct deep links still work.
- Run fresh direct tsc, vitest, production build, and the relevant Chromium/WebKit browser check.
- Update authoritative docs and handoff notes with the validation invariant and the remaining backend-security limitation.

push it to git review
