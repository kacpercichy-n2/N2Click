lets start a tier workflow

Release-hardening bundle 5: centralize scheduling and date invariants.

Context: an older audit reported a 92-day timeline bypass and clamp-back placement collisions. The shared `periodError`/`MAX_TASK_PERIOD_DAYS` reducer guards were implemented later, so first re-run the reproductions against current main. Do not reimplement already-fixed date validation. Parent-project containment is not currently a product invariant and must not be invented unattended.

Current baseline — already fixed, do not redo or weaken:
- `main` must contain commits `4bb7f69`, `86aa3e6`, and `11f1dea`; otherwise stop as stale.
- `SAVE_TASK` and `SET_TASK_DATES` already reject invalid/reversed/>92-day periods, and cross-day `SET_BLOCK_TIME` rejects extensions past 92 days. Preserve their tests and identity-preserving multi-block save behavior.
- Hard invariant 3 in `CLAUDE.md` is authoritative: overload is a warning, and same-person time collision blocks only the calendar drag/resize path. TaskModal allocation edits may deliberately create overlaps rendered side-by-side. Do not globally change this policy or rewrite migration/imported data without explicit human approval.
- Preserve the current window-owned bin drag lifecycle, synchronous drag refs, rendered-column hit testing, invalid-target feedback, and all `browser-check-bin-drag` scenarios.

Scope:
- Audit current write paths against existing shared period helpers. Add code only for a demonstrated remaining bypass; add regression coverage without duplicating helpers when the old report is already fixed.
- Audit automatic placement writers that call `nextFreeStart` (new allocation row, reassignment, right-click insertion). Where an automatic placement would clamp backwards into existing timed work near midnight, use a real free-slot search or reject atomically. Do not apply this rule to deliberate TaskModal edits or migration normalization without a separate approved policy change.
- Keep project/task date containment at its documented current behavior. If stricter parent containment is desired, write a concrete decision note and test matrix for human approval rather than choosing and enforcing a new policy in this unattended run.
- When an action is rejected, retain the prior state and give the UI a persistent Polish reason where the existing interaction can display one.
- Update only automatic-placement tests that currently bless end-of-day clamp-back overlap. Do not rewrite tests covering deliberate TaskModal allocation edits or defensive migration normalization.

Acceptance:
- The existing timeline/date guard reproduction is verified and remains green; no duplicate validation layer is introduced.
- Automatically placed new/inserted/reassigned blocks never gain a hidden overlap because of end-of-day clamp-back; impossible automatic placement is rejected clearly.
- Existing calendar move/resize/bin drag behavior and TaskModal overlap policy remain unchanged.
- Parent/child containment is either left unchanged with an explicit decision note or implemented only after prior human approval exists in the repo.
- Add focused reducer/time utility/browser coverage for all reported bypasses.
- Run fresh direct tsc, vitest, production build, Chromium and WebKit checks; update docs/handoff notes.

push it to git review
